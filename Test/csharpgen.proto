//
// This file is not used directly by the code generator,
// but the options defined here are used by it.
//

import "google/protobuf/descriptor.proto";

package CSharpGen;

//extend google.protobuf.FileOptions {
//	optional string my_file_option = 50000;
//}
extend google.protobuf.MessageOptions {
	//Class namespace
    optional string namespace = 50000;

    //Class access type
	optional string access = 50001 [default=public];

	//Call the instance methods BeforeSerialize and AfterDeserialize
    optional bool triggers = 50002 [default=false];

    //Store unrecognized fields during deserialization that
    // will be written back when serializing the class
    optional bool preserveunknown = 50003 [default=false];

    //Only create serialization code
    //Don't create the class being serialized
    //Useful when serializing classes from a third party DLL
    optional bool external = 50004 [default=false];

    //Serialization code is already generated elsewhere
    //Call that code for serialization.
    optional bool imported = 50005 [default=false];

    //Make object a class, struct or interface
    optional string type = 50006 [default=class];
}

extend google.protobuf.FieldOptions {
	optional string access = 50000 [default=public];
    
    //Define the field type in the generated code.
    optional string codetype = 50001;
	
    //Generate property for this field
	//If false, the a field or property must be implemented elsewhere for the code to compile.
	optional bool generate = 50002 [default=true];
    
    //Generate a c# readonly field
    optional bool readonly = 50003 [default=false];
}
//extend google.protobuf.EnumOptions {
//	optional bool my_enum_option = 50003;
//}
//extend google.protobuf.EnumValueOptions {
//	optional UInt32 my_enum_value_option = 50004;
//}
//extend google.protobuf.ServiceOptions {
//	optional MyEnum my_service_option = 50005;
//}
//extend google.protobuf.MethodOptions {
//	optional MyMessage my_method_option = 50006;
//}
