﻿
// Generated by ProtocolBuffer
// - a pure c# code generation implementation of protocol buffers
// Report bugs to: https://silentorbit.com/protobuf/

// DO NOT EDIT
// This file will be overwritten when CodeGenerator is run.
// To make custom modifications, edit the .proto file and add //:external before the message line
// then write the code and the changes in a separate file.

using System;
using System.IO;
using System.Text;
using System.Collections.Generic;

using SilentOrbit.ProtocolBuffers;
using static SilentOrbit.ProtocolBuffers.ProtocolParser;

// This is the backend code for reading and writing
namespace Proto.Test;

public partial class LongMessage
{
    /// <summary>Helper: create a new instance to deserializing into</summary>
    public static LongMessage Deserialize(Stream stream)
    {
        var instance = new LongMessage();
        Deserialize(stream, instance);
        return instance;
    }

    /// <summary>Helper: create a new instance to deserializing into</summary>
    public static LongMessage DeserializeLengthDelimited(Stream stream)
    {
        var instance = new LongMessage();
        DeserializeLengthDelimited(stream, instance);
        return instance;
    }

    /// <summary>Helper: create a new instance to deserializing into</summary>
    public static LongMessage DeserializeLength(Stream stream, int length)
    {
        var instance = new LongMessage();
        DeserializeLength(stream, length, instance);
        return instance;
    }

    /// <summary>Helper: put the buffer into a MemoryStream and create a new instance to deserializing into</summary>
    public static LongMessage Deserialize(byte[] buffer)
    {
        var instance = new LongMessage();
        using (var ms = new MemoryStream(buffer))
            Deserialize(ms, instance);
        return instance;
    }

    /// <summary>Helper: put the buffer into a MemoryStream before deserializing</summary>
    public static global::Proto.Test.LongMessage Deserialize(byte[] buffer, global::Proto.Test.LongMessage instance)
    {
        using (var ms = new MemoryStream(buffer))
            Deserialize(ms, instance);
        return instance;
    }

    /// <summary>Takes the remaining content of the stream and deserialze it into the instance.</summary>
    public static global::Proto.Test.LongMessage Deserialize(Stream stream, global::Proto.Test.LongMessage instance)
    {
        while (true)
        {
            int keyByte = stream.ReadByte();
            if (keyByte == -1)
                break;
            var key = ReadKey((byte)keyByte, stream);

            // Reading field ID > 16 and unknown field ID/wire type combinations
            switch (key.Field)
            {
                case 0:
                    throw new ProtocolBufferException("Invalid field id: 0, something went wrong in the stream");
                case 32:
                    if(key.WireType != Wire.Varint)
                        break;
                    instance.FieldX1 = (int)ReadUInt64(stream);
                    continue;
                case 64:
                    if(key.WireType != Wire.Varint)
                        break;
                    instance.FieldX2 = (int)ReadUInt64(stream);
                    continue;
                case 96:
                    if(key.WireType != Wire.Varint)
                        break;
                    instance.FieldX3 = (int)ReadUInt64(stream);
                    continue;
                case 100:
                    if(key.WireType != Wire.Varint)
                        break;
                    instance.FieldX4 = (int)ReadUInt64(stream);
                    continue;
                default:
                    SkipKey(stream, key);
                    break;
            }
        }

        return instance;
    }

    /// <summary>Read the VarInt length prefix and the given number of bytes from the stream and deserialze it into the instance.</summary>
    public static global::Proto.Test.LongMessage DeserializeLengthDelimited(Stream stream, global::Proto.Test.LongMessage instance)
    {
        long limit = ReadUInt32(stream);
        limit += stream.Position;
        while (true)
        {
            if (stream.Position >= limit)
            {
                if (stream.Position == limit)
                    break;
                else
                    throw new ProtocolBufferException("Read past max limit");
            }
            int keyByte = stream.ReadByte();
            if (keyByte == -1)
                throw new System.IO.EndOfStreamException();
            var key = ReadKey((byte)keyByte, stream);

            // Reading field ID > 16 and unknown field ID/wire type combinations
            switch (key.Field)
            {
                case 0:
                    throw new ProtocolBufferException("Invalid field id: 0, something went wrong in the stream");
                case 32:
                    if(key.WireType != Wire.Varint)
                        break;
                    instance.FieldX1 = (int)ReadUInt64(stream);
                    continue;
                case 64:
                    if(key.WireType != Wire.Varint)
                        break;
                    instance.FieldX2 = (int)ReadUInt64(stream);
                    continue;
                case 96:
                    if(key.WireType != Wire.Varint)
                        break;
                    instance.FieldX3 = (int)ReadUInt64(stream);
                    continue;
                case 100:
                    if(key.WireType != Wire.Varint)
                        break;
                    instance.FieldX4 = (int)ReadUInt64(stream);
                    continue;
                default:
                    SkipKey(stream, key);
                    break;
            }
        }

        return instance;
    }

    /// <summary>Read the given number of bytes from the stream and deserialze it into the instance.</summary>
    public static global::Proto.Test.LongMessage DeserializeLength(Stream stream, int length, global::Proto.Test.LongMessage instance)
    {
        long limit = stream.Position + length;
        while (true)
        {
            if (stream.Position >= limit)
            {
                if (stream.Position == limit)
                    break;
                else
                    throw new ProtocolBufferException("Read past max limit");
            }
            int keyByte = stream.ReadByte();
            if (keyByte == -1)
                throw new System.IO.EndOfStreamException();
            var key = ReadKey((byte)keyByte, stream);

            // Reading field ID > 16 and unknown field ID/wire type combinations
            switch (key.Field)
            {
                case 0:
                    throw new ProtocolBufferException("Invalid field id: 0, something went wrong in the stream");
                case 32:
                    if(key.WireType != Wire.Varint)
                        break;
                    instance.FieldX1 = (int)ReadUInt64(stream);
                    continue;
                case 64:
                    if(key.WireType != Wire.Varint)
                        break;
                    instance.FieldX2 = (int)ReadUInt64(stream);
                    continue;
                case 96:
                    if(key.WireType != Wire.Varint)
                        break;
                    instance.FieldX3 = (int)ReadUInt64(stream);
                    continue;
                case 100:
                    if(key.WireType != Wire.Varint)
                        break;
                    instance.FieldX4 = (int)ReadUInt64(stream);
                    continue;
                default:
                    SkipKey(stream, key);
                    break;
            }
        }

        return instance;
    }

    /// <summary>Serialize the instance into the stream</summary>
    public static void Serialize(Stream stream, LongMessage instance)
    {
        using (var msField = new MemoryStream())
        {
            // Key for field: 32, Varint
            stream.WriteByte(128);
            stream.WriteByte(2);
            WriteUInt64(stream,(ulong)instance.FieldX1);
            // Key for field: 64, Varint
            stream.WriteByte(128);
            stream.WriteByte(4);
            WriteUInt64(stream,(ulong)instance.FieldX2);
            // Key for field: 96, Varint
            stream.WriteByte(128);
            stream.WriteByte(6);
            WriteUInt64(stream,(ulong)instance.FieldX3);
            // Key for field: 100, Varint
            stream.WriteByte(160);
            stream.WriteByte(6);
            WriteUInt64(stream,(ulong)instance.FieldX4);
        }
    }

    /// <summary>Helper: Serialize into a MemoryStream and return its byte array</summary>
    public static byte[] SerializeToBytes(LongMessage instance)
    {
        using (var ms = new MemoryStream())
        {
            Serialize(ms, instance);
            return ms.ToArray();
        }
    }
    /// <summary>Helper: Serialize with a varint length prefix</summary>
    public static void SerializeLengthDelimited(Stream stream, LongMessage instance)
    {
        var data = SerializeToBytes(instance);
        WriteUInt32(stream, (uint)data.Length);
        stream.Write(data, 0, data.Length);
    }
}

